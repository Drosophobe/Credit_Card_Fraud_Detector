service: "service:ccf_service" 
labels:
   owner: bentoml-team
   stage: dev
include:
- "*.py"
- "*.ipynb"  # A pattern for matching which files to include in the bento
python:
   packages:  # Additional pip packages required by the service
   - scikit-learn
   - pandas
   - numpy
